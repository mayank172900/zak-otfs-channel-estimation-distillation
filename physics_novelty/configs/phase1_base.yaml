inherits: ../../configs/system.yaml

paths:
  results_dir: results
  logs_dir: logs
  report_dir: report

phase1_dataset:
  split: train
  mode: full
  total_size: 48000
  training_snr_db: 15.0
  training_pdr_db: [0.0, 5.0, 20.0, 25.0, 30.0, 35.0]
  modulation: bpsk
  materialize_dtype: float16
  include_physics_target: true
  baseline_checkpoint_path: ../logs/checkpoints/full_cnn_best.pt
